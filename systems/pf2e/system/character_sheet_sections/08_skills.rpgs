character_sheet_section skills() =
  section(
    id = "skills_section",
    show_divider = true,
    content_item_min_width = 1,
    collapsible = true,
    header = [
      icon(
        id = "skills_section_icon",
        name = "ic_skills",
        size = "medium",
        margin_top = 8,
        margin_right = 8,
        margin_left = 16,
      ),
      text(
        id = "skills_section_title",
        margin_right = 16,
        margin_left = 16,
        text = "Skills",
        style = "title",
      ),
    ],
    content = [
      stat(
        id = "skill_acrobatics",
        stat = "acrobatics",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "acrobatics", statName = "Acrobatics")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "acrobatics", statName = "Acrobatics")),
        style = "list",
        stat_color = acrobatics_color,
        name_color = acrobatics_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_arcana",
        stat = "arcana",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "arcana", statName = "Arcana")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "arcana", statName = "Arcana")),
        style = "list",
        stat_color = arcana_color,
        name_color = arcana_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_athletics",
        stat = "athletics",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "athletics", statName = "Athletics")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "athletics", statName = "Athletics")),
        style = "list",
        stat_color = athletics_color,
        name_color = athletics_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_crafting",
        stat = "crafting",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "crafting", statName = "Crafting")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "crafting", statName = "Crafting")),
        style = "list",
        stat_color = crafting_color,
        name_color = crafting_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_deception",
        stat = "deception",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "deception", statName = "Deception")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "deception", statName = "Deception")),
        style = "list",
        stat_color = deception_color,
        name_color = deception_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_diplomacy",
        stat = "diplomacy",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "diplomacy", statName = "Diplomacy")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "diplomacy", statName = "Diplomacy")),
        style = "list",
        stat_color = diplomacy_color,
        name_color = diplomacy_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_intimidation",
        stat = "intimidation",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "intimidation", statName = "Intimidation")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "intimidation", statName = "Intimidation")),
        style = "list",
        stat_color = intimidation_color,
        name_color = intimidation_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_medicine",
        stat = "medicine",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "medicine", statName = "Medicine")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "medicine", statName = "Medicine")),
        style = "list",
        stat_color = medicine_color,
        name_color = medicine_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_nature",
        stat = "nature",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "nature", statName = "Nature")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "nature", statName = "Nature")),
        style = "list",
        stat_color = nature_color,
        name_color = nature_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_occultism",
        stat = "occultism",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "occultism", statName = "Occultism")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "occultism", statName = "Occultism")),
        style = "list",
        stat_color = occultism_color,
        name_color = occultism_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_performance",
        stat = "performance",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "performance", statName = "Performance")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "performance", statName = "Performance")),
        style = "list",
        stat_color = performance_color,
        name_color = performance_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_perception",
        stat = "perception",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "perception", statName = "Perception")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "perception", statName = "Perception")),
        style = "list",
        stat_color = perception_color,
        name_color = perception_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_religion",
        stat = "religion",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "religion", statName = "Religion")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "religion", statName = "Religion")),
        style = "list",
        stat_color = religion_color,
        name_color = religion_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_society",
        stat = "society",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "society", statName = "Society")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "society", statName = "Society")),
        style = "list",
        stat_color = society_color,
        name_color = society_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_stealth",
        stat = "stealth",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "stealth", statName = "Stealth")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "stealth", statName = "Stealth")),
        style = "list",
        stat_color = stealth_color,
        name_color = stealth_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_survival",
        stat = "survival",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "survival", statName = "Survival")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "survival", statName = "Survival")),
        style = "list",
        stat_color = survival_color,
        name_color = survival_label_color,
        always_show_sign = true,
      ),
      stat(
        id = "skill_thievery",
        stat = "thievery",
        change_amount = 0,
        on_tap_event = event(name = "skillTap", payload = payload(stat = "thievery", statName = "Thievery")),
        on_long_tap_event = event(name = "editSkillProficiency", payload = payload(stat = "thievery", statName = "Thievery")),
        style = "list",
        stat_color = thievery_color,
        name_color = thievery_label_color,
        always_show_sign = true,
      ),
      resourceSection(
        id = "skills_lore_skills",
        resource_stat = "all_lore_skills",
        resource_set_stat = "lore_skills",
        resource_id = "lore_skill",
        header_view = text(
          id = "skills_lore_skills_header",
          text = "Lore skills",
          style = "text",
          bold = true,
          margin_left = 16,
          margin_top = 8,
        ),
        is_visible = !isEmpty(all_lore_skills),
      ),
    ],
  );
