mechanic reset_actions(
  event_names = "refresh_actions",
) =
  sequence(
    effects = [
      setStat(
        stat = "actions_remaining",
        new_value = max_actions_per_turn,
        aggregation_type = "set",
      ),
      setStat(
        stat = "reaction_available",
        new_value = max_reactions_per_round,
        aggregation_type = "set",
      ),
    ],
  );
