mechanic on_refocus(event_names = "refocus") =
  sequence(
    effects = [
      setStat(
        stat = "focus_points",
        new_value = min([effective_max_focus_points, focus_points + 1]),
        aggregation_type = "set",
      ),
    ],
  );
