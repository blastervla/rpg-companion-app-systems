mechanic spell_toggle_prepare(name = "Toggle spell preparation", event_names = "togglePrepareSpell") =
  when {
    can_toggle_preparation -> setStat(
      stat = "prepared",
      new_value = !prepared,
      aggregation_type = "set",
    ),
  };
