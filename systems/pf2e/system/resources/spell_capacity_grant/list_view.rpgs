view spell_capacity_grant_list() =
  listItem(
    id = "spell_capacity_grant_list",
    title = display_description,
    subtitle = if restriction_mode == "none" then "" else restriction_mode_name,
  );
