mechanic spell_cast(name = "Cast spell", event_names = "castSpell") =
  roll(
    rolls = [
      rollable(
        formulas = [spell_attack_roll_formula],
        text = "to hit",
      ),
    ],
  );
